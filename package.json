{
  "name": "cloud-function-http-trigger-slack-webhook",
  "version": "1.0.0",
  "description": "Cloud function with http trigger type. Send message to Slack channel via webhook",
  "dependencies": {
    "dotenv": "^12.0.0"
  }
}